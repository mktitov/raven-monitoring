Описание типов узлов образующих цепочки

* Узлы относящиеся к DataSource

*----------------------------------------+-----------------------+
||Тип узла                               || Краткое описание     |
*----------------------------------------+-----------------------+ 
| AttributeValueDataSourceNode           | Источник, формирующий данные на основе значения своего атрибута value
*----------------------------------------+-----------------------+
| DatabaseRecordReaderNode               | Формирует записи на основе выполнения SQL запроса к БД и отправляет их потребителю, запросившему данные
*----------------------------------------+-----------------------+
| FileStreamSourceNode                   | Отправляет потрибителям файл, загруженный в атрибут file, в виде потока (java.io.InputStream)
*----------------------------------------+-----------------------+ 
| FileReaderNode                         | Считывает файл или набор файлов по заданному URL и передает их потребителям в виде потока байтов
*----------------------------------------+-----------------------+ 
| FileDataSource                         | Источник передающий потребителям данные файла, загруженного в атрибут file
*----------------------------------------+-----------------------+ 
| LdapReaderNode                         | Формирует таблицу на основе данных полученных от LDAP сервера и отправляет ее своим потребителям
*----------------------------------------+-----------------------+ 
| NodeScannerNode                        | Позволяет отсканировать заданную ветку дерева на предмет узлов, удовлетворяющих определенному условию, и передать результат сканирования потребителям в виде таблицы
*----------------------------------------+-----------------------+
| SnmpReaderNode                         | Собирает данные по SNMP протоколу и отправляет своим потребителям 
*----------------------------------------+-----------------------+ 
| SyslogMessageHandlerNode               | Обрабатывает syslog сообщение и отправляет его ввиде записи своим потребителям
*----------------------------------------+-----------------------+ 
| SmbFileReader                          | Позволяет считывать файлы по SMB протоколу и передавать их потребителю ввиде потока байтов (java.io.InputStream)
*----------------------------------------+-----------------------+ 

* Узлы относящиеся к DataPipe

*----------------------------------------+-----------------------+
||Тип узла                               || Краткое описание     |
*----------------------------------------+-----------------------+ 
| AsyncDataPipe                          | Отправляет данные, поступившие от источника, своим потребителям в асинхронном режиме
*----------------------------------------+-----------------------+
| ChangeRouteNode                        | Позволяет изментить направление движения данных
*----------------------------------------+-----------------------+
| CollectionComposerNode                 | Позволяет скомпоновать поступающие данные в коллекцию (java.util.Collection) и отправить ее потребителям
*----------------------------------------+-----------------------+
| CollectionDecomposerNode               | Получив данные в виде набора элементов (Collection, Iterator, Iterable, org.raven.table.Table), отсылает каждый элемент отдельно
*----------------------------------------+-----------------------+
| CsvLineToRecordNode                    | Преобразует входящую строку в CSV фаормате к записи
*----------------------------------------+-----------------------+
| CsvRecordWriterNode                    | Преобразовывает поток записей в CSV формат
*----------------------------------------+-----------------------+
| ExcelTableReaderNode                   | Преобразует входящий поток данных (в формате excel) в таблицу
*----------------------------------------+-----------------------+
| InitiatePullDataNode                   | При поступлении данных на данный узел, инициирует получение данных из другой цепочки. Данные полученные от источника и другой цепочки микшируются и отправляются потребителям
*----------------------------------------+-----------------------+
| InitiatePushDataNode                   | Кроме отправления данных своим потребителям, узел может инициировать передачу данных в другую цепочку
*----------------------------------------+-----------------------+
| JxlsReportNode                         | Генерирует отчет в виде excel файла
*----------------------------------------+-----------------------+
| HttpSessionNode                        | На основе входящих данных, позволяет выполнить цепочку http get/post запросов и отправить результат выполнения http запросов потребителям
*----------------------------------------+-----------------------+
| MailWriterNode                         | Для каждой полученной порции данных формирует и отправляет сообщение по электронной почте
*----------------------------------------+-----------------------+
| NetworkScannerNode                     | Позволяет просканировать диапазоны ip адресов и вернуть их в виде таблицы
*----------------------------------------+-----------------------+
| NodeGeneratorNode                      | Позволяет создавать дочерние узлы на основе табличных данных полученных от источника данных
*----------------------------------------+-----------------------+
| RecordGeneratorPipeNode                | Канал (pipe) формирующий записи на основе данных получаемых от источника (data source)
*----------------------------------------+-----------------------+
| RecordsAggregatorNode                  | Получает набор записей, агрегирует, отсылает записи с агрегированной информацией потребителям
*----------------------------------------+-----------------------+
| RecordsAsCrossTableNode                | Позволяет сформировать перекрестную таблицу из набора записей поступивших от источника
*----------------------------------------+-----------------------+
| RecordsToTableNode                     | Преобразует входящие данные (записи, заданной схемы) в таблицу и отправляет ее потребителям
*----------------------------------------+-----------------------+
| RecordValidatorNode                    | Проводит проверку записи, поступившей на вход
*----------------------------------------+-----------------------+
| RRDataSource                           | Узел конфигурирующий datasource для round robin database
*----------------------------------------+-----------------------+
| SafeDataPipeNode                       | Получает данные от источника данных и передает их потребителям (threadsafe)
*----------------------------------------+-----------------------+
| SchedulableDataPipe                    | Позволяет по расписанию получать данные от источника (dataSource) и передавить их, связанным с данным узлом потребителям
*----------------------------------------+-----------------------+
| StreamCacheDataPipeNode                | Получает на вход java.io.InputStream, кеширует, используя TemporaryFileManager, и отдает подтребителям в виде javax.activation.DataSource
*----------------------------------------+-----------------------+
| TableMultiplexerNode                   | Позволяет из нескольких таблиц, предоставляемых дочерними источниками объектов, создать одну таблицу
*----------------------------------------+-----------------------+
| TableSummaryNode                       | На основе входящей таблицы формирует новую, добавляя дополнительные строки и/или колонки
*----------------------------------------+-----------------------+
| TextToLinesNode                        | Разбивает входящие текстовые данные (InputStream, String) на строки
*----------------------------------------+-----------------------+
| XslTransformerNode                     | Транасформирует XML поток, используя XSL  и результат отправляет потребителям
*----------------------------------------+-----------------------+


* Узлы относящиеся к DataConsumer

*----------------------------------------+-----------------------+
||Тип узла                               || Краткое описание     |
*----------------------------------------+-----------------------+ 
| DataConsumerActionNode                 | Выполняет действие над набором данных полученных от источника данных
*----------------------------------------+-----------------------+
| DatabaseRecordWriterNode               | Сохраняет записи поступающие от источника данных в таблице БД
*----------------------------------------+-----------------------+
| ReferenceNodeManager                   | Создает ссылки на узлы (ReferenceNode) полученные в виде таблицы от источника данных
*----------------------------------------+-----------------------+
| RecordsAsTableNode                     | Получает набор записей от источника данных и отображает их ввиде таблицы
*----------------------------------------+-----------------------+
| SdbQueryResultDataDef                  | Позволяет сформировать значения для графика от источника, предоставляющего результат выполнения запроса к статистической БД
*----------------------------------------+-----------------------+
| SvnWriterNode                          | Позволяет сохранить данные в репозитарии subversion
*----------------------------------------+-----------------------+
| TableViewNode                          | Позволяет просматривать данные в виде таблицы
*----------------------------------------+-----------------------+

